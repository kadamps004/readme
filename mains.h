#ifndef __MAINS__H
#define __MAINS__H

void remove_activate(GtkApplication *app, Window_grid *window_grid);
void add_activate(GtkApplication *app, Window_grid *win_grid);
void view_activate(GtkApplication *app, Window_grid *window_grid);
void search_activate(GtkApplication *app, Window_grid *window_grid);

#endif
